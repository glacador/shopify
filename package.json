{
  "name": "shopify-monorepo",
  "private": true,
  "scripts": {
    "build": "cd nailscope-app && npm install && npm run build",
    "dev": "cd nailscope-app && npm run dev",
    "start": "cd nailscope-app && npm run start"
  }
}
